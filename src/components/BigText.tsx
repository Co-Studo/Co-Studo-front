import styled from 'styled-components';

const BigText = styled.span`
  font-weight: 500;
  font-size: 1.5rem;
  white-space: nowrap;
  line-height: 1.7;
  margin-bottom: 1rem;
`;

export default BigText;
